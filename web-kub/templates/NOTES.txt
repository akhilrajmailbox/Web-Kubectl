1. Get the application URL by running these commands:

{{- if contains "LoadBalancer" .Values.service.type }}
  export SERVICE_URL=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "web-kub.fullname" . }} -o jsonpath='{.items[0].status.loadBalancer.ingress[0].hostname}')
  echo "You have to configure your consumer with : http://${SERVICE_URL}:{{ $.Values.service.port }}"
{{- end }}